import { HospitalpipePipe } from './hospitalpipe.pipe';

describe('HospitalpipePipe', () => {
  it('create an instance', () => {
    const pipe = new HospitalpipePipe();
    expect(pipe).toBeTruthy();
  });
});
